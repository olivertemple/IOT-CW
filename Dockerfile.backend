FROM node:20-alpine

WORKDIR /app

# Install dependencies (production)
COPY package*.json ./
RUN npm ci --omit=dev

# Copy project files
COPY . .

EXPOSE 3001

# Run the backend server
CMD ["node", "server.cjs"]
